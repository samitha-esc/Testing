# Core dependencies
opencv-python-headless
numpy
mido
python-rtmidi

# Note: picamera2 is pre-installed on Raspberry Pi OS. 

# TODO: Install MediaPipe separately later for the hybrid approach
# mediapipe